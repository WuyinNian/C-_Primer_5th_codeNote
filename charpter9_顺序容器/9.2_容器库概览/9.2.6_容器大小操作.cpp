#include <iostream>
#include <cctype>
#include <string>
#include <vector>
#include <array>
#include <list>
using namespace std;

/* 
    size():返回容器中元素的数目
    empty()：size为0时返回true
    max_size()：返回一个大于或等于该类型容器所能容纳的最大元素数的值

    例外：forwa_list不支持size
 */
void test1()
{
}
int main(int argc, const char *argv[])
{
    test1();
    return 0;
}