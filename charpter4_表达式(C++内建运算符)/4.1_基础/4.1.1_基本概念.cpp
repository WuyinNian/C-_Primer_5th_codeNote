#include <iostream>
#include <cctype>
#include <string>
#include <vector>
using namespace std;

void test1()
{
    //一元运算符：* &
    //二元运算符：作用于两个运算对象
    //三元运算符
    //函数调用是一种特殊的运算符，对运算的对象没有限制
}

void test2()
{
    //左值和右值
    //当对象用作右值时，使用的是对象的值（内容），用作对象的左值时，用的是对象的身份（内存空间）
}
int main(int argc, const char *argv[])
{
    test1();
    return 0;
}